# Cloudflare Pages Configuration via Wrangler

name = "gk88-khuyenmai"
compatibility_date = "2024-01-01"

[env.production]
# Production environment variables
# Sẽ override bằng Cloudflare Dashboard → Settings → Environment variables

pages_build_output_dir = "out"

# Deploy settings
account_id = "" # Sẽ tự động lấy khi login

