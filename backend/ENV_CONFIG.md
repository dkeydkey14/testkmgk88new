# Backend Environment Configuration

Tạo file `.env` trong folder `backend/`:

```env
PORT=3782
FRONTEND_URL=http://localhost:3781
```

## Giải thích:

- **PORT**: Port backend API server (mặc định 3782)
- **FRONTEND_URL**: URL của frontend để cấu hình CORS

